# This template is copied into .woodpecker/smoke.yaml in the separate
# target-application repository during Lab 02.
when:
  - event: push

steps:
  - name: local-registry-smoke-test
    image: localhost:5000/lab/alpine:3.21
    commands:
      - echo "Woodpecker pipeline is running on the Kubernetes backend."
      - cat /etc/alpine-release
